[project]
name = "birdnet-warren"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "birdnet-analyzer[all,gui]",
]

[tool.uv.sources]
birdnet-analyzer = { git = "https://github.com/birdnet-team/BirdNET-Analyzer.git" }
